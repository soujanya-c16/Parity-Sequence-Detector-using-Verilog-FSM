module test_seq;
  reg x, clk, reset;
  wire z;
  seq_detector uut(
    .x(x),
    .clk(clk),
    .reset(reset),
    .z(z)
  );
  
   initial begin
        clk = 0;
        forever #5 clk = ~clk;
    end
  
  initial
    begin
      $dumpfile("seq.vcd");
      $dumpvars(0, test_seq);
      reset = 0;
      x=0;
      #10
       reset = 1;
  
    #10 x = 0;
        #10 x = 1;
        #10 x = 1;
        #10 x = 0;

        #10 x = 1;
        #10 x = 0;
        #10 x = 1;
        #10 x = 1;

        #10 x = 1;
        #10 x = 1;
        #10 x = 0;
        #10 x = 0;

        #20;
        $finish;
    end

endmodule
